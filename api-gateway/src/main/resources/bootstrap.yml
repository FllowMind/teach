spring:
  application:
    name: api-gateway
  cloud:
    nacos:
      config:
        server-addr: 192.168.206.128:8848 #Nacos配置中心地址
        file-extension: yaml #文件拓展格式
      discovery:
        server-addr: 192.168.206.128:8848

  profiles:
    active: dev